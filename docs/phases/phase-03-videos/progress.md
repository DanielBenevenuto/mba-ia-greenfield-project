# phase-03-videos — Progress

**Status:** completed
**SIs:** 13/13 completed

**Definition of Done (verified 2026-09-20):**

| Check | Command | Result |
|---|---|---|
| Type-check | `npx tsc --noEmit` | exit 0 |
| Lint | `npm run lint` | exit 0 (0 errors, 45 pre-configured warnings) |
| Unit + integration | `npm test -- --runInBand` | 260 passed / 37 suites |
| E2E | `npm run test:e2e` | 79 passed / 5 suites |
| Re-runnability | full suite twice against the same DB | green both times |

**Follow-ups for a future phase (out of Phase 03 scope):**

- Abandoned upload sessions are recoverable but not reaped automatically: a video left in `draft` with a non-null `upload_id` keeps its multipart session open in storage. A scheduled cleanup (or an S3 lifecycle rule on incomplete multipart uploads) belongs with the management panel in Fase 04.
- There is no endpoint to reprocess a video stuck in `failed`; today it requires re-enqueuing the job manually. The processor is already idempotent, so this is a thin endpoint when Fase 04 adds the management surface.
- `S3_PUBLIC_ENDPOINT` defaults to the Compose hostname, which is right for container-to-container use and for the test suite. A browser running on the host needs it pointed at `http://localhost:9000`; this becomes relevant when the Fase 04/05 frontend consumes presigned URLs directly.

### SI-03.1 — Instalar dependências e criar configuração de storage e fila
- **Status:** completed
- **Tests:** 23 passing
- **Observations:**
  - `.env.example` shipped `MAIL_FROM="StreamTube" <noreply@streamtube.com>` with the angle brackets outside the quotes, which Docker Compose's `.env` parser rejects (`unexpected character "<" in variable name`) — `docker compose up` failed before any Phase 03 work started. Fixed by quoting the whole value, exactly as `nestjs-project/CLAUDE.md` § "Environment File Conventions" prescribes.
  - `src/config/env.validation.integration-spec.ts` had to gain the three newly-required storage keys in its `requiredEnv` fixture; without that, the pre-existing SWAGGER_ENABLED tests would fail for an unrelated reason.

### SI-03.2 — Subir MinIO, Redis e ffmpeg na infraestrutura Docker
- **Status:** completed
- **Tests:** no tests (Infra) — ACs verified manually
- **Observations:**
  - `minio/minio` is no longer pullable from Docker Hub (`pull access denied ... repository does not exist`). The community AGPLv3 image now lives on Quay, so the Compose file pins `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z` and `quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z`.
  - Verified in-container: `ffprobe 5.1.9`, and `ffmpeg -protocols` lists `https` — the prerequisite for TD-06 reading the source through a presigned URL instead of downloading 10GB.
  - `minio-init` exits 0 on a re-run (`mc mb --ignore-existing`), so repeated `docker compose up -d` is safe.

### SI-03.3 — Criar entidade Video, enum de status e migration
- **Status:** completed
- **Tests:** 10 passing (SI scope); full suite 171 passing on two consecutive runs
- **Observations:**
  - DG-1 closed: `migrations.integration-spec.ts` now drops the managed enum types in `beforeAll`. Verified by running the complete suite twice in a row against the same database — 171/171 both times (previously 144 then 143).
  - Adding `Channel.videos` (`@OneToMany`) forced `Video` into the entity array of all 10 test DataSources that register `Channel`; without it TypeORM fails with "Entity metadata for Channel#videos was not found". `cleanAllTables` also had to delete `videos` before `channels` to respect the FK.
  - The migration was generated by the TypeORM CLI (never hand-written) and its `down()` already drops `videos_status_enum`.

### SI-03.4 — Implementar o gerador de identificador público único
- **Status:** completed
- **Tests:** 5 passing
- **Observations:**
  - Zero-dependency generator: `nanoid@6` is ESM-only and would break the CommonJS build, so `node:crypto` + a rejection-sampling base62 encoder is used instead (rejection sampling avoids the modulo bias that would over-represent the first 8 symbols).

### SI-03.5 — Implementar o StorageService com multipart e URLs pré-assinadas
- **Status:** completed
- **Tests:** 17 passing
- **Observations:**
  - Two `S3Client` instances: internal endpoint for server-to-server calls, public endpoint for signing URLs handed to clients. SigV4 signs the Host header, so one client cannot serve both roles.
  - The multipart integration test uploads a real 5 MiB part through a presigned URL against the Compose MinIO — verified by listing the bucket afterwards.

### SI-03.6 — Expor lookup de canal por usuário no ChannelsService
- **Status:** completed
- **Tests:** 4 passing (added to the existing channels suite)
- **Observations:**
  - `findByUserId` lives in `ChannelsService` so the videos module never queries the `channels` table directly (DG-2 resolution).

### SI-03.7 — Implementar pré-cadastro em rascunho e abertura da sessão de upload
- **Status:** completed
- **Tests:** 19 passing
- **Observations:**
  - `startUpload` opens the multipart session before persisting and aborts it if the insert fails, so a failed draft never leaves an orphan session holding storage.
  - The `public_id` insert retries on unique violation: 62^11 makes a collision practically impossible, but the unique index is the real guarantee the capability asks for.

### SI-03.8 — Implementar conclusão e abort do upload com enfileiramento do processamento
- **Status:** completed
- **Tests:** 30 passing
- **Observations:**
  - **Library revision recorded on TD-02.** `@nestjs/bullmq@12.0.0` is published as `"type": "module"` with its `require` condition pointing at the same ESM bundle, so it cannot be required from this CommonJS project (`SyntaxError: Unexpected token 'export'`). Pinned `@nestjs/bullmq@^11.0.5`, which is CJS and whose peer range already accepts `bullmq ^6`.
  - `ioredis` became an **optional** peer in BullMQ 6 and must be installed explicitly, otherwise the queue fails at runtime with "BullMQ could not load the optional 'ioredis' package". Added `ioredis@^5.9.0`.
  - The integration test enqueues against the real Redis and reads the job back with `queue.getJobs`, asserting payload and retry options.

### SI-03.9 — Expor os endpoints do handshake de upload
- **Status:** completed
- **Tests:** 13 passing (e2e)
- **Observations:**
  - E2E drives the real handshake: `POST /videos/uploads` → `PUT` the part straight to MinIO via the presigned URL → `POST .../complete`. The file never traverses the API.
  - One test declares exactly 10 GiB and asserts the returned part plan covers it (160 parts at the 64 MiB default), proving the ceiling is reachable without uploading the bytes.

### SI-03.10 — Implementar extração de metadados e geração de thumbnail
- **Status:** completed
- **Tests:** 8 passing
- **Observations:**
  - `ffprobe`/`ffmpeg` are invoked with `execFile` (no shell) and receive a **presigned URL** as input, so a 10GB source is read over HTTP Range instead of being downloaded to the worker disk.
  - The test fixture is generated at runtime with `ffmpeg -f lavfi -i testsrc`; no binary video is committed to the repository.
  - Thumbnail extraction falls back to the first frame when the requested offset is past the end of a short clip.

### SI-03.11 — Implementar o worker de vídeo em processo separado
- **Status:** completed
- **Tests:** 9 passing
- **Observations:**
  - `WorkerModule` must register `Video`, `Channel` and `User` in `forFeature`: `autoLoadEntities` only picks up what `forFeature` declares, and TypeORM needs the whole relation closure of `Video` or it fails with "Entity metadata for Video#channel was not found".
  - Verified manually end to end with the containers running: upload → worker → `ready` with `duration_seconds=5.000`, 640x480 h264 and the thumbnail object written to `thumbnails/{id}/poster.jpg`.
  - The worker log shows it correctly no-ops jobs whose video row was deleted — the idempotency path exercised in production, not just in tests.

### SI-03.12 — Expor metadados, streaming com Range e download
- **Status:** completed
- **Tests:** 12 unit + 14 e2e passing
- **Observations:**
  - Public playback routes are `@Public()` plus a new `OptionalJwtAuthGuard` (added to the auth module, where JWT verification belongs): a missing/invalid token means anonymous, a valid one lets the owner see their own non-ready videos.
  - Non-ready videos answer `404` to non-owners so the endpoint does not leak their existence; the owner gets `409 VIDEO_NOT_READY`.
  - Verified against the live API with curl: `Range: bytes=0-1023` → `206` with `Content-Range: bytes 0-1023/34200` and exactly 1024 bytes; no Range → `200`; bad range → `416`; download → `302` to a presigned URL.

### SI-03.13 — Atualizar OpenAPI e a documentação de IA
- **Status:** completed
- **Tests:** 13 passing (openapi-export integration, extended)
- **Observations:**
  - `openapi.json` regenerated and asserted by tests: all six video paths present, error statuses referencing the shared `ApiErrorEnvelope`, `206`/`416` documented on the streaming route, and the playback routes carrying no bearer requirement while the upload routes do.
  - IC-1 closed at the source: the testing guide's "Object Storage" section now describes MinIO (with the presigned-URL host-rewrite gotcha and the fixture generator) and "Message Queue" is no longer TBD — it documents BullMQ + Redis and the two version constraints found during SI-03.8.
  - `docs/diagrams/software-arch.mermaid` and the root `CLAUDE.md` no longer say "TBD" for the queue.
  - **Inherited lint debt surfaced.** `npm run lint` was already failing on `main`: 133 errors in Phase 02 files this phase never touched (verified by linting pristine copies extracted with `git show main:<file>`), almost all `no-unsafe-*` on supertest's `any`-typed `res.body`. Resolved per the user's decision with a scoped `eslint.config.mjs` override that disables the type-aware `no-unsafe-*` / `require-await` / `unbound-method` rules for `*.spec.ts`, `*.integration-spec.ts` and `*.e2e-spec.ts` only — production code keeps every rule at full strength and `tsc --noEmit` still type-checks the specs. The 9 remaining errors (all inherited, all in non-spec files) were fixed properly rather than suppressed: typed narrowing in `channels.service.ts`, a real type for the `entities` parameter in `create-test-data-source.ts`, and two dead variables.
  - Two test-only defects surfaced when the full suite ran together and were fixed at the root:
    - `videos.module.spec.ts` needed a **global** `ConfigModule` once `VideosModule` started importing `AuthModule` (→ `MailModule`), exactly as `.claude/rules/nestjs-testing.md` § "Modules with forRootAsync + ConfigType" warns.
    - The queue tests were racing the **running** `video-worker` container: it drained jobs before the integration assertion could see them in `waiting`, and it also wrote to rows the E2E suites were deleting (which produced a one-off failure in `videos-playback.e2e-spec.ts`). Fixed at the root by pausing the queue for the duration of those suites and resuming in teardown, so the tests are deterministic whether or not the worker container is up. Verified by three consecutive full unit/integration runs and five consecutive E2E runs with the worker live, plus an explicit check that the queue is not left paused afterwards.
